\chapter*{Input/Output}
\addcontentsline{toc}{chapter}{Input/Output}
This chapter presents the Twin-64 I/O architecture and introduces its core concepts related to the processor and system architecture. It does not describe any specific I/O module, nor does it cover operating-system initialization or runtime I/O procedures.

\section*{Concepts}
\addcontentsline{toc}{section}{Concepts}
Twin-64 uses a memory-mapped I/O architecture. A dedicated portion of the physical address space in address region zero is reserved exclusively for the I/O subsystem. This clean partitioning ensures predictable system behavior during early initialization and simplifies both hardware implementation and software design.

The I/O subsystem is organized around functional entities known as \textbf{modules}. Typical modules include processors, memory controllers, and various I/O devices. All modules connect to the \textbf{system bus}, which supports up to 64 modules in total. Although real systems often provide fewer physical slots, the architecture itself imposes no such restriction. At system startup, each module is assigned a dedicated 4-KB physical address page. This page defines the base address range to which the module responds and serves as a fixed anchor for its control and status information.

Within each module, functionality is exposed through a collection of \textbf{registers}. These registers are accessed using standard load and store instructions, although they are not required to be 64 bits wide. A core set of registers is common across all modules, establishing a uniform interface for essential operations. Each module may define additional device-specific registers to implement its specialized capabilities. 

Many modules also require additional storage to manage their internal \textbf{I/O elements}. An I/O element—also called a \textbf{device} is the smallest addressable functional unit within a module. For example, a console terminal module may expose two I/O elements: one for input and one for output.

The system bus spans a 256-KB address space, aligned to a 256-KB boundary, with one 4-KB page reserved for every possible module. In practice, physical constraints often limit the number of installed modules, but the architecture supports growth by allowing multiple modules to reside on a single I/O card, with each card occupying one physical slot on the bus.

Overall, the Twin-64 I/O subsystem emphasizes clarity, modularity, and extensibility. Its hierarchical structure—from modules to devices, and from hard physical pages to optional soft and broadcast regions—supports both straightforward configurations and more complex, densely populated systems.

\section*{I/O Address Space}
\addcontentsline{toc}{section}{I/O Address Space}

 As shown in the overall physical memory layout, the physical memory address range dedicated to I/O modules is located from \texttt{0xFF\_0000\_0000} to \texttt{0xFF\_FFFF\_FFFF}. The following figure depicts the I/O memory address space ranges.

\begin{figure}[H]
\begin{center}
\begin{tikzpicture}[scale=0.7, transform shape]
        
  	\draw[help lines, gray!70, dashed] (0,0) grid(18,16);

	% Rectangle dimensions
	\def\width{8}
	\def\leftofs{4}
	\def\rightofs{\leftofs + \width}

	% Left side
	\draw[thick, dashed] (\leftofs, 0) -- (\leftofs, 1);
	\draw[thick] (\leftofs, 1) -- (\leftofs, 5); 
	\draw[thick, dashed] (\leftofs, 5) -- (\leftofs, 11);
	\draw[thick] (\leftofs, 11) -- (\leftofs, 15);
	\draw[thick, dashed] (\leftofs, 15) -- (\leftofs, 16);
	

	% Right side
	\draw[thick, dashed] (\rightofs, 0) -- (\rightofs, 1);
	\draw[thick] (\rightofs, 1) -- (\rightofs, 5); 
	\draw[thick, dashed] (\rightofs, 5) -- (\rightofs, 11);
	\draw[thick] (\rightofs, 11) -- (\rightofs, 15); 	
	\draw[thick, dashed] (\rightofs, 15) -- (\rightofs, 16);
	
		
	% Edges
	\draw[thick] (\leftofs, 14) -- (\rightofs, 14);
	\draw[thick] (\leftofs, 12) -- (\rightofs, 12);
	\draw[thick] (\leftofs, 4) -- (\rightofs, 4); 
	\draw[thick] (\leftofs, 2) -- (\rightofs, 2); 
	
	% Labels 
	\node[left] at (\leftofs, 14 - 0.2) {\texttt{0xFF\_0000\_0000}};
	\node[left] at (\leftofs, 12 + 0.2) {\texttt{0xFF\_00FF\_FFFF}};
	\node[left] at (\leftofs, 12 - 0.2) {\texttt{0xFF\_0100\_0000}};
	
	\node[left] at (\leftofs, 4 + 0.2) {\texttt{0xFF\_FFEF\_FFFF}};
	\node[left] at (\leftofs, 4 - 0.2) {\texttt{0xFF\_FFF0\_0000}};
	
	\node[left] at (\leftofs, 2 + 0.2) {\texttt{0xFF\_FFFF\_FFFF}};
	
	\node at (\leftofs + \width / 2, 13) {\textbf{Processor dependent Code}};
	\node at (\leftofs + \width / 2, 8) {\textbf{Soft physical address space}};
    \node at (\leftofs + \width / 2, 3) {\textbf{Hard physical address space}};
	
	%allocation arrow
	\draw[thick, -> ] (\leftofs - 1, 5) -- (\leftofs - 1, 10); 
	\node[anchor = east] at (\leftofs - 1.25, 8.5) {\textbf{Allocate}};
	
	% bracket
	\draw[thick]  [decorate, decoration={brace, amplitude=0.5cm, mirror}] (\rightofs + 0.5,2) -- (\rightofs + 0.5,14)
	node[midway, right=1cm, anchor=west]{\Large I/O Address Range};
	
\end{tikzpicture}
\end{center}
\caption{I/O Address Space}
\label{fig:I/O Address Space}
\end{figure}

The I/O address range is a 4 Gbyte area at the end of the 40-bit physical address space. The area is divided into several ranges. The first range contains the \textbf{processor dependent code}. There is a set of library functions to provide low-level primitives for the boot process and the operating system. 

At the upper level of the I/O address range there is the \textbf{hard physical address} range. It contains a page for each module connected at the system bus level. Up to 256 modules can be allocated. The last module slot is the broadcast HPA. At system reset or reboot, there is only the broadcast HPA. The processor will try to locate all other modules in the physical address range. Finally, each I/O module will then allocate its required soft physical address range. Each module therefore listens to three distinct address ranges:

\begin{itemize}
    \item \textbf{Hard physical address range:}  
    This is the module’s assigned physical page at system initialization. Its position is determined by the card’s physical slot and the module’s index on the I/O card. Access to the hard physical page is privileged and reserved for supervisor-level operations.

    \item \textbf{Soft physical address range:}  
    Certain modules require more than a single page of address space to support their full operational state. For such cases, additional memory pages may be dynamically allocated in the soft physical region, providing the necessary flexibility without compromising layout consistency.

    \item \textbf{Broadcast address range:}  
    A write issued through the broadcast HPA address updates the corresponding register in \emph{every} module on the bus. Broadcast writes are useful for system-wide resets, synchronization operations, or events that must be delivered simultaneously to all modules.
\end{itemize}

\section*{Hard physical address range}
\addcontentsline{toc}{section}{Hard physical address range}

The \textbf{hard physical address} range contains an I/O page for each I/O module installed. There is room for 256 I/O modules in the system bus. Each module has a hard physical address page, which consists of up to 16 register sets of 32 registers each. A register itself is a 64-bit word. The first register set is the supervisory register set. This set is architected and must be implemented by each module. It contains the I/O module description. The remainder of the registers in this set and the organization of the remaining register set is specific to the I/O module. 

\begin{figure}[H]
\begin{center}
\begin{tikzpicture}[scale=0.7, transform shape]
        
  	% \draw[help lines, gray!70, dashed] (0,0) grid(16,12);

	% Rectangle dimensions
	\def\width{8}
	\def\leftofs{4}
	\def\rightofs{\leftofs + \width}

	% Left side
	\draw[thick] (\leftofs, 0) -- (\leftofs, 3); 
	\draw[thick, dashed] (\leftofs, 3) -- (\leftofs, 7);
	\draw[thick] (\leftofs, 7) -- (\leftofs, 12);
	
	% Right side
	\draw[thick] (\rightofs, 0) -- (\rightofs, 3); 
	\draw[thick, dashed] (\rightofs, 3) -- (\rightofs, 7);
	\draw[thick] (\rightofs, 7) -- (\rightofs, 12); 		
		
	% Edges
	\draw[thick] (\leftofs, 0) -- (\rightofs, 0);
	\draw[thick] (\leftofs, 2) -- (\rightofs, 2);
	\draw[thick] (\leftofs, 8) -- (\rightofs, 8);
	\draw[thick] (\leftofs, 10) -- (\rightofs, 10);
	\draw[thick] (\leftofs, 12) -- (\rightofs, 12);
	
	% Labels 
	\node[left] at (\leftofs, 12 - 0.2) {\texttt{0x0}};
	\node[left] at (\leftofs, 10 - 0.2) {\texttt{0x80}};
	\node[left] at (\leftofs, 8 - 0.2) {\texttt{0x100}};
	\node[left] at (\leftofs, 2 - 0.2) {\texttt{0xF00}};
	\node[left] at (\leftofs, 0  + 0.2) {\texttt{0xFFF}};
	
	\node at (\leftofs + \width / 2, 11) {\textbf{Supervisor Register Set}};
	\node at (\leftofs + \width / 2, 9) {\textbf{Register Set 1}};
	\node at (\leftofs + \width / 2, 1) {\textbf{Register Set 15}};

		
	% bracket
	\draw[thick]  [decorate, decoration={brace, amplitude=0.5cm, mirror}] (\rightofs + 0.5,0) -- (\rightofs + 0.5,12)
	node[midway, right=1cm, anchor=west]{\Large HPA Page};
	
\end{tikzpicture}
\end{center}
\caption{Hard physical Address Page}
\label{fig:Hard physical Address Page}
\end{figure}


\section*{Broadcast address range}
\addcontentsline{toc}{section}{Broadcast address range}

I/O module 255, i.e. the last page in the HPA address range, is the broadcast address range. It is actually not a module and contains no actual functions. Writes issued to broadcastable registers using this module number are not directed to a single module. Instead, the write operation is applied simultaneously to all I/O modules on the system bus. This mechanism is typically used for system-wide commands such as resets, synchronization events, or configuration updates that must reach every module at once.

At reset time, all modules only listen to their broadcastable registers. Only after system bus setup and module discovery, is the HPA address range available.

\section*{Soft physical address range}
\addcontentsline{toc}{section}{Soft physical address range}

In many cases, a module requires more addressable registers or data structures than can be  
accommodated within its hard physical address page. The \textbf{soft physical address} range provides a flexible region where additional space may be allocated for an I/O module’s extended functionality. From a software perspective, the allocated area is termed \textbf{I/O Element}. However, the meaning of register sets in this range is entirely up to the module. A module responds to memory accesses within its configured soft physical address space, which may reside  either in the I/O address space or the general memory address space.

\begin{figure}[H]
\begin{center}
\begin{tikzpicture}[scale=0.7, transform shape]
        
  	% \draw[help lines, gray!70, dashed] (0,0) grid(18,16);

	% Rectangle dimensions
	\def\width{8}
	\def\leftofs{4}
	\def\rightofs{\leftofs + \width}

	% Left side
	\draw[thick, dashed] (\leftofs, 0) -- (\leftofs, 1);
	\draw[thick] (\leftofs, 1) -- (\leftofs, 15); 
	\draw[thick, dashed] (\leftofs, 15) -- (\leftofs, 16);
	

	% Right side
	\draw[thick, dashed] (\rightofs, 0) -- (\rightofs, 1);
	\draw[thick] (\rightofs, 1) -- (\rightofs, 15); 
	\draw[thick, dashed] (\rightofs, 15) -- (\rightofs, 16);
	
	%Fills
	\node[anchor=north west, fill=gray!30, minimum width=8cm,
  		  minimum height=0.75cm,] at (\leftofs, 10.5) { };
  		  
  	\node[anchor=north west, fill=gray!30, minimum width=8cm,
  		  minimum height=0.25cm,] at (\leftofs, 3.25) { };
		
	% Edges
	\draw[thick] (\leftofs, 14) -- (\rightofs, 14);
	\draw[thick] (\leftofs, 12) -- (\rightofs, 12);
	\draw[thick] (\leftofs, 10.5) -- (\rightofs, 10.5);
	\draw[thick] (\leftofs, 9.75) -- (\rightofs, 9.75);
	\draw[thick] (\leftofs, 4) -- (\rightofs, 4); 
	\draw[thick] (\leftofs, 3.25) -- (\rightofs, 3.25);
	\draw[thick] (\leftofs, 3) -- (\rightofs, 3);
	\draw[thick] (\leftofs, 2) -- (\rightofs, 2); 
	
	% Labels 
	\node[left] at (\leftofs, 14 - 0.2) {\texttt{0xFF\_0000\_0000}};
	\node[left] at (\leftofs, 12 + 0.2) {\texttt{0xFF\_0FFF\_FFFF}};
	\node[left] at (\leftofs, 12 - 0.2) {\texttt{0xFF\_1000\_0000}};
	\node[left] at (\leftofs, 4 - 0.2) {\texttt{0xFF\_FFF0\_0000}};
	\node[left] at (\leftofs, 2 + 0.2) {\texttt{0xFF\_FFFF\_FFFF}};
		
	%allocation arrow
	\draw[thick, -> ] (\leftofs, 3.15) -- (\leftofs - 1, 3.15) --
	(\leftofs - 1, 10.5 ) -- (\leftofs, 10.5 ); 
	
	% brackets
	\draw[thick]  [decorate, decoration={brace, amplitude=0.2cm, mirror}] (\rightofs + 0.5,12) -- (\rightofs + 0.5,14)
	node[midway, right=0.75cm, anchor=west]{\Large PDC};
	
	\draw[thick]  [decorate, decoration={brace, amplitude=0.2cm, mirror}] (\rightofs + 0.5,4) -- (\rightofs + 0.5,12)
	node[midway, right=0.75cm, anchor=west]{\Large SPA};
	
	\draw[thick]  [decorate, decoration={brace, amplitude=0.2cm, mirror}] (\rightofs + 0.5,2) -- (\rightofs + 0.5,4)
	node[midway, right=0.75cm, anchor=west]{\Large HPA};
	
\end{tikzpicture}
\end{center}
\caption{I/O Address Space}
\label{fig:SPA Address Space Allocation}
\end{figure}

For example, a terminal I/O controller has a hard physical page in the I/O address space and a SPA address allocated for its terminal ports. As a another example, a memory controller has a hard physical page in the I/O address space, yet its soft physical region typically encompasses all or part of the system’s physical memory. Similarly, a processor may allocate a soft physical address range within the I/O space to store performance counters or diagnostic information that needs to be accessible without interfering with normal memory traffic.


\section*{Supervisor Register Set}
\addcontentsline{toc}{section}{Supervisor Register Set}

The supervisor registers implements the architected set of registers for configuring and managing the I/O module. The \texttt{REG 0} to \texttt{REG 7} are implemented by each module. \texttt{REG 8} to \texttt{REG 31} are module specific.

\begin{figure}[H]
\begin{center}
\begin{tikzpicture}[x=1cm, y=1cm, scale=0.7, transform shape]
        
  	% \draw[help lines, gray!70, dashed] (0,0) grid(12,17);
  	
  	% Rectangle dimensions
	\def\width{8}
	\def\leftofs{3}
	\def\rightofs{\leftofs + \width}
  	\def\middleofs{ \leftofs + \width / 2 }
  	
  	% Bits
  	\node at (\leftofs   + 0.25,16 + 0.25) {\small 63};
  	\node at (\middleofs + 0.25,16 + 0.25) {\small 32};
	\node at (\middleofs - 0.25,16 + 0.25) {\small 31};
	\node at (\middleofs + 1.75,16  + 0.25) {\small 16};
	\node at (\middleofs + 2.25,16 + 0.25) {\small 15};
	\node at (\rightofs  - 0.25,16 + 0.25) {\small 0};

	% Box
	\draw[thick] (\leftofs, 0) -- (\leftofs, 3);
	\draw[thick, dashed] (\leftofs, 3) -- (\leftofs, 7);
	\draw[thick] (\leftofs, 7) -- (\leftofs, 16);
	\draw[thick] (\rightofs, 0) -- (\rightofs, 3);
	\draw[thick, dashed] (\rightofs, 3) -- (\rightofs, 7);
	\draw[thick] (\rightofs, 7) -- (\rightofs, 16);
	\draw[thick] (\leftofs, 0) -- (\rightofs, 0);
	\draw[thick] (\leftofs, 16) -- (\rightofs, 16);
	
	% Fields 
	% IOREG0
	\draw[thick] (\leftofs, 15) -- (\rightofs, 15);
  	\node[anchor = east] at ({\leftofs - 0.75}, {15 + 0.5}) { IOREG0 };
  	\node at ( \middleofs, {15 + 0.5}) { IO\_STATUS };	
  	
  	% IOREG1
  	\draw[thick] (\leftofs, 14) -- (\rightofs, 14);
  	\node[anchor = east] at ({\leftofs - 0.75}, {14 + 0.5}) { IOREG1 };
  	\node at ( \middleofs, {14 + 0.5}) { IO\_COMMAND };
  	
  	% IOREG2
  	\draw[thick] (\leftofs, 13) -- (\rightofs, 13);
  	\node[anchor = east] at ({\leftofs - 0.75}, {13 + 0.5}) { IOREG2 };
  	\node at ( \middleofs, {13 + 0.5}) { IO\_CONFIG };

  	% IOREG3
  	\draw[thick] (\leftofs, 12) -- (\rightofs, 12);
  	\node[anchor = east] at ({\leftofs - 0.75}, {12 + 0.5}) { IOREG3 };
  	\node at ( \middleofs, {12 + 0.5}) { IO\_VERSION };
  	
  	% IOREG4
  	\draw[thick] (\leftofs, 11) -- (\rightofs, 11);
    \node[anchor = east] at ({\leftofs - 0.75}, {11 + 0.5}) { IOREG4 };
    \node at ( \middleofs, {11 + 0.5}) { IO\_SPA\_ADR };
  
  	% IOREG5
  	\draw[thick] (\leftofs, 10) -- (\rightofs, 10);
  	\node[anchor = east] at ({\leftofs - 0.75}, {10 + 0.5}) { IOREG5 };
  	\node at ( \middleofs, {10 + 0.5}) { IO\_RESERVED\_1 };
  	
  	% IOREG6
  	\draw[thick] (\leftofs, 9) -- (\rightofs, 9);
  	\node[anchor = east] at ({\leftofs - 0.75}, {9 + 0.5}) { IOREG6 };
  	\node at ( \middleofs, {9 + 0.5}) { IO\_RESERVED\_2 };

  	% IOREG7
  	\draw[thick] (\leftofs, 8) -- (\rightofs, 8);
  	\node[anchor = east] at ({\leftofs - 0.75}, {8 + 0.5}) { IOREG7 };  
  	\node at ( \middleofs, {8 + 0.5}) { IO\_RESERVED\_3 };


	\node at ( \middleofs, {5 + 0.5}) { module specific };
	\draw[thick] (\leftofs, 2) -- (\rightofs, 2);
  	
  	% IOREG30
  	\draw[thick] (\leftofs, 1) -- (\rightofs, 1);
  	\node[anchor = east] at ({\leftofs - 0.75}, {1 + 0.5}) { IOREG30 };
  	\node at ( \middleofs, {1 + 0.5}) { module specific };
  	
  	% IOREG31
  	\draw[thick] (\leftofs, 0) -- (\rightofs, 0);
  	\node[anchor = east] at ({\leftofs - 0.75}, {0 + 0.5}) { IOREG31 };
  	\node at ( \middleofs, {0 + 0.5}) { module specific };
			
\end{tikzpicture}
\end{center}
\caption{I/O Module Supervisory Registers}
\label{fig:I/O Module Supervisory Registers}
\end{figure}
The following table lists the architected registers for an I/O module. Access to these registers is always on a double word boundary.

\begin{longtable}{@{}p{0.35\linewidth}p{0.5\linewidth}@{}}
    \caption{Supervisor Register Set} \\
    \toprule
    \textbf{Reg} & \textbf{Purpose} \\
    \midrule
    \endfirsthead
    \toprule
    \textbf{Reg} & \textbf{Purpose} \\
    \midrule
    \endhead
    \midrule
    \multicolumn{2}{r}{\textit{Continued on next page}} \\
    \midrule
    \endfoot
    \bottomrule
    \endlastfoot
    \textbf{IO\_STATUS} & I/O module status \\
    \midrule
    \textbf{IO\_COMMAND} & I/O module command and EIR data \\
    \midrule
    \textbf{IO\_CONFIG} & I/O module configuration data \\
    \midrule
    \textbf{IO\_VERSION} & I/O module version data \\
    \midrule
    \textbf{IO\_SPA\_ADR\_0} & I/O module soft physical address range \\
    \midrule
    \textbf{IO\_RESERVED\_1} & reserved for future use \\
    \midrule
    \textbf{IO\_RESERVED\_2} & reserved for future use \\
    \midrule
    \textbf{IO\_RESERVED\_3} & reserved for future use \\
    \midrule
\end{longtable}


\begin{itemize}


\item \textbf{I/O Status Register}. 

\begin{center}
\drawInstrFormatSixtyFour
        {0,  16}
        {0 }
\end{center}

\item \textbf{I/O Command Register}

\begin{center}
\drawInstrFormatSixtyFour
        {0,  16}
        {0 }
\end{center}

\item \textbf{I/O Configuration Register}

\begin{center}
\drawInstrFormatSixtyFour
        {0, 15, 16}
        {0, 0 }
\end{center}

\item \textbf{I/O Version Register}

\begin{center}
\drawInstrFormatSixtyFour
        {0,  16}
        {0 }
\end{center}

\item \textbf{I/O Soft Physical Address}

\begin{center}
\drawInstrFormatSixtyFour
        {0,  16}
        {0 }
\end{center}

\item \textbf{I/O Reserved Register 1 to 3}

... reserved for future use


\end{itemize}


\section*{I/O Elements}
\addcontentsline{toc}{section}{I/O Elements}

Modules typically consist of I/O elements. They represent the smallest I/O unit of operation.

??? a subset of registers, at least status, command and data.

??? several types of I/O Elements.

??? a two page type, where one page is privileged and in I/O space, another one mapped into user space. 

??? also option of packing many smaller I/O elements in one page.

 

\section*{Interrupt Processing}
\addcontentsline{toc}{section}{Interrupt Processing}

A processor has two control registers. The external interrupt register and the external interrupt mask register. Both are ANDed and checked after each instruction execution. An I/O module can set a bit in the processor external interrupt request register. There are 64 interrupt groups in the EIRR control register. When not masked by the EIMR register, the interrupt is handled.

When an I/O operation is started, the command contains in addition to the command a 32-bit field, which provides the target processor HPA page address and the bit position to set in the EIRR register of the target processor. This part of the command register is normally set up at system initialization time, but can also be set for each I/O request. 

\begin{center}
\drawInstrFormatSixtyFour
        {0, 1.5, 8, 16}
        {EIR-Bit, ProcHpa, command data}
\end{center}


\section*{Design Rationale}
\addcontentsline{toc}{section}{Design Rationale}

??? simple but flexible design

??? a rather large address range, forward looking 

??? closely modeled after PA-RISC, broadcast is simplified

??? key requirement, is that all modules have a description on board in a ROM.

??? PDC lives at the system level, literally as a ROM on the board ?








