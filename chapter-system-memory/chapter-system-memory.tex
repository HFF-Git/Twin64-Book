\chapter*{System Memory}
\addcontentsline{toc}{chapter}{System Memory}


\section*{Virtual Address Space}
\addcontentsline{toc}{section}{Virtual Address Space}

Twin-64 features a 52-bit virtual address range, organized into a 20-bit segment identifier and a 32-bit offset within that segment. The following figure illustrates the structure of a virtual address and how the segment is selected.

\begin{figure}[H]
\begin{center}
\begin{tikzpicture}[scale=0.9, transform shape]
        
  	\draw[help lines, gray!70, dashed] (0,0) grid(16,14);
        
    \node[  tsRectangle, 
                minimum width=12cm,
                minimum height=1cm,
                fill=white!50] (blocks) at (6,13) { };
                
  	\draw[-] (6,12.5) -- (6, 13.5);
    \draw[-] (2,12.5) -- (2, 13.5);
    
    \node at (11.75,13.75) {0};
	\node at (6.25,13.75) {31};
	\node at (5.75,13.75) {32};
	\node at (2.25,13.75) {51};
	\node at (1.75,13.75) {52};
	\node at (0.25,13.75) {63};           	
    \node at (1,13) {0};
    \node at (4,13) {segId};
    \node at (9,13) {offset};
    	        
    \def\leftofs{9}
	\foreach \i in {0,...,3} {
  		\pgfmathsetmacro\x{ - \i  + \leftofs}
  		\pgfmathsetmacro\y{\i }
  		\draw[tsRectangle, fill=white] (\x,\y) rectangle ++( 6, 8);
	}
		
	\draw[->, thick] (9,12.5) -- (9,9);
	\draw[->, thick] (4,12.5) -- (4,11) -- (5.75,11);
	
	\node[  tsRectangle, 
            minimum width=1cm,
            minimum height=0.255cm,
            anchor=west,
            fill=white!50] (blocks) at (9,8.875) { };
		
	\node at (\leftofs, 7) {Segment};
		
\end{tikzpicture}
\end{center}
\caption{Virtual Memory Address Range}
\label{fig:Virtual Memory Address Range}
\end{figure}

A virtual address is translated into a physical address. For translation purposes, the virtual address range can also be viewed as a set of virtual pages. The architected page size is 16 KB, with a 38-bit virtual page number and a 14-bit page offset. 

\section*{Address Space Layout}
\addcontentsline{toc}{section}{Address Space Layout}

Twin-64 architecture features a 36-bit physical memory address range which allows a maximum physical memory of up to 64 GBytes.  

\begin{figure}[ht]
\begin{center}
\begin{tikzpicture}[scale=0.9, transform shape]
        
  	\draw[help lines, gray!70, dashed] (0,0) grid(17,16);

	% Rectangle dimensions
	\def\width{8}
	\def\leftofs{3}
	\def\rightofs{\leftofs + \width}

	% Left side
	\draw[thick] (\leftofs, 0) -- (\leftofs, 1);
	\draw[thick, dashed] (\leftofs, 1) -- (\leftofs, 6); 
	\draw[thick] (\leftofs, 6) -- (\leftofs, 8);
	\draw[thick, dashed] (\leftofs, 8) -- (\leftofs, 10); 	
	\draw[thick] (\leftofs, 10) -- (\leftofs, 14);
	\draw[thick, dashed] (\leftofs, 14) -- (\leftofs, 15); 	
	\draw[thick] (\leftofs, 15) -- (\leftofs, 16);

	% Right side
	\draw[thick] (\rightofs, 0) -- (\rightofs, 1);
	\draw[thick, dashed] (\rightofs, 1) -- (\rightofs, 6); 
	\draw[thick] (\rightofs, 6) -- (\rightofs, 8);
	\draw[thick, dashed] (\rightofs, 8) -- (\rightofs, 10); 	
	\draw[thick] (\rightofs, 10) -- (\rightofs, 14);
	\draw[thick, dashed] (\rightofs, 14) -- (\rightofs, 15); 	
	\draw[thick] (\rightofs, 15) -- (\rightofs, 16);
			
	% Edges
	\draw[thick] (\leftofs, 0) -- (\rightofs, 0); 
	\draw[thick] (\leftofs, 7) -- (\rightofs, 7); 
	\draw[thick] (\leftofs, 11) -- (\rightofs, 11);
	\draw[thick] (\leftofs, 13) -- (\rightofs, 13);
	\draw[thick] (\leftofs, 16) -- (\rightofs, 16);
	
	
	% Labels 
	\node[left] at (\leftofs, 16 - 0.2) {\texttt{0x00\_0000\_0000}};
	\node[left] at (\leftofs, 13 + 0.2) {\texttt{0x00\_EFFF\_FFFF}};	
	\node[left] at (\leftofs, 13 - 0.2) {\texttt{0x00\_F000\_0000}};
	\node[left] at (\leftofs, 11 + 0.2) {\texttt{0x00\_FFFF\_FFFF}};
	\node[left] at (\leftofs, 11 - 0.2) {\texttt{0x01\_0000\_0000}};
	\node[left] at (\leftofs, 7 + 0.2) {\texttt{0x0F\_FFFF\_FFFF}};
	\node[left] at (\leftofs, 7 - 0.2) {\texttt{0x10\_0000\_0000}};
	\node[left] at (\leftofs, 0 + 0.2) {\texttt{0xFF\_FFFF\_FFFF\_FFFF}};
		
	\node at (\leftofs + \width / 2, 14.5) {\textbf{Physical Memory Address Space}};
	\node at (\leftofs + \width / 2, 12) {\textbf{I/O Address Space}};
	\node at (\leftofs + \width / 2, 9) {\textbf{Physical Memory Address Space}};
	\node at (\leftofs + \width / 2, 4) {\textbf{Virtual Memory Address Space}};
	
	% bracket
	\draw[thick]  [decorate, decoration={brace, amplitude=0.5cm, mirror}] (\rightofs + 0.5,7) -- (\rightofs + 0.5,16)
	node[midway, right=1cm, anchor=west]{Physical Address range};
	
	\draw[thick]  [decorate, decoration={brace, amplitude=0.5cm, mirror}] (\rightofs + 0.5,0) -- (\rightofs + 0.5,7)
	node[midway, right=1cm, anchor=west]{Virtual Address range};

\end{tikzpicture}
\end{center}
\caption{Memory Address Ranges}
\label{fig:Memory Address Ranges}
\end{figure}

Segment IDs 0 to 15 represent the physical address space, with a maximum size of 64 GB. These segments are directly mapped to physical addresses, without TLB translation or access-rights checking. Only privileged-mode code can access these segments. Control Register 0 contains a field that encodes the actual memory size installed in the system.

Twin-64 uses a memory-mapped I/O architecture. The I/O address space occupies the top 256 MB of the first segment. I/O modules allocate their registers and storage within this range, and access to this area is uncached.

The remaining address space is dedicated to virtual memory. Virtual storage is allocated dynamically, and the TLB provides the necessary translation to physical addresses while enforcing access rights.
