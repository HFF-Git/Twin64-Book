\chapter*{System Memory}
\addcontentsline{toc}{chapter}{System Memory}


\section*{Virtual Address Space}
\addcontentsline{toc}{section}{Virtual Address Space}

Twin-64 features a 52-bit virtual address range, organized into a 20-bit region identifier and a 32-bit offset within that region. The smallest unit of access is one byte. The following figure illustrates the structure of a virtual address and how the region data is selected.

\begin{figure}[H]
\begin{center}
\begin{tikzpicture}[scale=0.7, transform shape]
        
  	% \draw[help lines, gray!70, dashed] (0,0) grid(16,14);
        
    \node[  tsRectangle, 
                minimum width=12cm,
                minimum height=1cm,
                fill=white!50] (blocks) at (6,13) { };
                
  	\draw[-] (6,12.5) -- (6, 13.5);
    \draw[-] (2,12.5) -- (2, 13.5);
    
    \node at (11.75,13.75) {0};
	\node at (6.25,13.75) {31};
	\node at (5.75,13.75) {32};
	\node at (2.25,13.75) {51};
	\node at (1.75,13.75) {52};
	\node at (0.25,13.75) {63};           	
    \node at (1,13) {0};
    \node at (4,13) {regionId};
    \node at (9,13) {offset};
    	        
    \def\leftofs{9}
	\foreach \i in {0,...,3} {
  		\pgfmathsetmacro\x{ - \i  + \leftofs}
  		\pgfmathsetmacro\y{\i }
  		\draw[tsRectangle, fill=white] (\x,\y) rectangle ++( 6, 8);
	}
		
	\draw[->, thick] (9,12.5) -- (9,9);
	\draw[->, thick] (4,12.5) -- (4,11) -- (5.75,11);
	
	\node[  tsRectangle, 
            minimum width=1cm,
            minimum height=0.255cm,
            anchor=west,
            fill=white!50] (blocks) at (9,8.875) { \small data };
		
	\node at (\leftofs, 7) {\LARGE Region};
		
\end{tikzpicture}
\end{center}
\caption{Virtual Memory Address Range}
\label{fig:Virtual Memory Address Range}
\end{figure}

A virtual address is translated into a physical address. For translation purposes, the virtual address range can also be viewed as a set of virtual pages. The architected page size is 4 KBytes, with a 40-bit virtual page number and a 12-bit page offset. 

\begin{figure}[H]
\begin{center}
\begin{tikzpicture}[scale=0.7, transform shape]
        
  	% \draw[help lines, gray!70, dashed] (0,0) grid(16,8);
  	
 	\def\vadrY{7}
 	\def\tlbY{4}
 	\def\padrY{0.5}
 	
 	% vAdr
  	\node[  tsRectangle, 
            minimum width=12cm,
            minimum height=1cm,
            fill=white] at (8,\vadrY) { };
                
  	\draw[-] (11.5,\vadrY - 0.5) -- (11.5, \vadrY + 0.5);
    \draw[-] (4,\vadrY - 0.5) -- (4, \vadrY + 0.5);
        
    \node at (13.75,\vadrY + 0.75) {0};
	\node at (11.75,\vadrY + 0.75) {11};
	\node at (11.25,\vadrY + 0.75) {12};
	\node at (4.25,\vadrY + 0.75) {51};
	\node at (3.75,\vadrY + 0.75) {52};
	\node at (2.25,\vadrY + 0.75) {63};           	
    \node at (3,\vadrY) {0};
    \node at (7,\vadrY) {Virtual page};
    \node at (12.5,\vadrY) {Page offset};
                
\end{tikzpicture}
\end{center}
\caption{Virtual page address}
\label{fig:Virtual page address}
\end{figure}



\section*{Address Space Layout}
\addcontentsline{toc}{section}{Address Space Layout}

Twin-64 architecture features a 52-bit address space with a 40-bit physical memory address range that allows a maximum physical memory of up to 1 TByte. The following figure depicts the overall address space layout.

\begin{figure}[H]
\begin{center}
\begin{tikzpicture}[scale=0.7, transform shape]
        
  	draw[help lines, gray!70, dashed] (0,0) grid(17,16);

	% Rectangle dimensions
	\def\width{8}
	\def\leftofs{3}
	\def\rightofs{\leftofs + \width}

	% Left side
	\draw[thick] (\leftofs, 0) -- (\leftofs, 1);
	\draw[thick, dashed] (\leftofs, 1) -- (\leftofs, 8); 
	\draw[thick] (\leftofs, 6) -- (\leftofs, 12); 	
	\draw[thick, dashed] (\leftofs, 12) -- (\leftofs, 15); 	
	\draw[thick] (\leftofs, 15) -- (\leftofs, 16);

	% Right side
	\draw[thick] (\rightofs, 0) -- (\rightofs, 1);
	\draw[thick, dashed] (\rightofs, 1) -- (\rightofs, 8); 
	\draw[thick] (\rightofs, 6) -- (\rightofs, 12);
	\draw[thick, dashed] (\rightofs, 12) -- (\rightofs, 15); 	
	\draw[thick] (\rightofs, 15) -- (\rightofs, 16);
			
	% Edges
	\draw[thick] (\leftofs, 0) -- (\rightofs, 0); 
	\draw[thick] (\leftofs, 9) -- (\rightofs, 9); 
	\draw[thick] (\leftofs, 11) -- (\rightofs, 11);
	\draw[thick] (\leftofs, 16) -- (\rightofs, 16);
	
	
	% Labels 
	\node[left] at (\leftofs, 16 - 0.2) {\texttt{0x0\_0000\_0000\_0000}};
	\node[left] at (\leftofs, 11 + 0.2) {\texttt{0x0\_00EF\_FFFF\_FFFF}};	
	\node[left] at (\leftofs, 11 - 0.2) {\texttt{0x0\_00FF\_0000\_0000}};
	\node[left] at (\leftofs, 9 + 0.2) {\texttt{0x0\_00FF\_FFFF\_FFFF}};
	\node[left] at (\leftofs, 9 - 0.2) {\texttt{0x0\_0100\_0000\_0000}};
	\node[left] at (\leftofs, 0 + 0.2) {\texttt{0xF\_FFFF\_FFFF\_FFFF}};
		
	\node at (\leftofs + \width / 2, 14.5) {\textbf{Physical Memory Address Space}};
	\node at (\leftofs + \width / 2, 10) {\textbf{I/O Address Space}};
	\node at (\leftofs + \width / 2, 4) {\textbf{Virtual Memory Address Space}};
	
	% bracket
	\draw[thick]  [decorate, decoration={brace, amplitude=0.5cm, mirror}] (\rightofs + 0.5,9) -- (\rightofs + 0.5,16)
	node[midway, right=1cm, anchor=west]{Physical Address range};
	
	\draw[thick]  [decorate, decoration={brace, amplitude=0.5cm, mirror}] (\rightofs + 0.5,0) -- (\rightofs + 0.5,9)
	node[midway, right=1cm, anchor=west]{Virtual Address range};

\end{tikzpicture}
\end{center}
\caption{Memory Address Ranges}
\label{fig:Memory Address Ranges}
\end{figure}

Region IDs 0 to 255 represent the physical address space, with a maximum size of 1 TB. These regions are directly mapped to physical addresses, without TLB translation or access-rights checking. Only privileged-mode code can access these regions. Control Register 0 contains a field that contains the actual physical memory size installed in the system.

Twin-64 uses a memory-mapped I/O architecture. The I/O address space occupies the top region, region 255, of the physical address space. I/O modules allocate addresses for their registers and storage within this range, and access to this area is uncached.

The remaining address space is dedicated to virtual memory. Virtual storage is allocated dynamically, and the TLB provides the necessary translation to physical addresses while enforcing access rights.
