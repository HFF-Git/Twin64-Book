%--------------------------------------------------------------------------------
%
%
%--------------------------------------------------------------------------------
\pagebreak
\section*{MFIA - Move from Instruction Address}
\addcontentsline{toc}{section}{MFIA - Move from Instruction Address}

%--------------------------------------------------------------------------------
\begin{iPageDescEntry}{Syntax}
\texttt{MFIA [.U/S/L/A] RegR}
\end{iPageDescEntry}

%--------------------------------------------------------------------------------
\begin{iPageDescEntry}{Format}
The \texttt{MFIA} instruction uses the register instruction format. \\

\begin{flushleft}
\drawInstrFormat
    {0,1,3,5,5.5, 6.5,16}
    {\OpCodeGrpSYS, \OpCodeMFIA, RegR, 1, mode, 0}
\end{flushleft}
\end{iPageDescEntry}

%--------------------------------------------------------------------------------
\begin{iPageDescEntry}{Description}
The \texttt{MFIA} instruction copies the current instruction address to register \texttt{RegR}.The mode field allows to select a portion of the instruction address. 

\begin{itemize}
	\item \texttt{.A} (mode = 0) — copies IA to \texttt{RegR}. This is the default operation.
    \item \texttt{.L} (mode = 1) — Deposits 20 bits of \texttt{val} into bits [31..12] of \texttt{RegR}.  
    \item \texttt{.S} (mode = 2) — Deposits 20 bits of \texttt{val} into bits [51..32] of \texttt{RegR}.  
    \item \texttt{.U} (mode = 3) — Deposits the lower 12 bits of \texttt{val} into bits [63..52] of \texttt{RegR}.
\end{itemize}
\end{iPageDescEntry}

%--------------------------------------------------------------------------------
\begin{iPageDescOpEntry}{Operation}
\begin{lstlisting}[style=iPageOpStyle]
RegR <- PSW.[IA]                       // .A mode (0)
RegR <- ((PSW.[IA] & 0xFFFFF) << 12)   // .L mode (1)
RegR <- ((PSW.[IA] & 0xFFFFF) << 32)   // .S mode (2)
RegR <- ((PSW.[IA] & 0xFFF)   << 52)   // .U mode (3)
\end{lstlisting}
\end{iPageDescOpEntry}

%--------------------------------------------------------------------------------
\begin{iPageDescEntry}{Exceptions}
None.
\end{iPageDescEntry}

%--------------------------------------------------------------------------------
\begin{iPageDescEntry}{Notes}
It would be beneficial to have the possibility to just a subject of the instruction address. How about options to get the segment, the page number in the segment and the status bits. It is very similar to LDIL fields.

??? unclear operation think about what is needed .....
\end{iPageDescEntry}
